# Fastlane Appfile — Android
# Credentials are supplied via environment variables (account: android_001).

json_key_file(ENV["ANDROID_JSON_KEY_FILE"])
package_name(ENV["ANDROID_PACKAGE_NAME"] || "com.appfactory.unitconverterpro")

# Factory account reference: android_001
