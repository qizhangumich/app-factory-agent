// Root build file — QR & Barcode Scanner+ (Android)
// Plugin versions are declared here and applied (without version) per-module.

plugins {
    id("com.android.application") version "8.5.2" apply false
    id("org.jetbrains.kotlin.android") version "1.9.24" apply false
}
