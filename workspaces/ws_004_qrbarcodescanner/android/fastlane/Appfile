# Appfile — QR & Barcode Scanner+ (Android / Google Play)
# Credentials are supplied via environment variables; nothing secret is
# committed. Account: android_001.

json_key_file(ENV["ANDROID_001_PLAY_JSON_KEY_PATH"])
package_name(ENV["ANDROID_PACKAGE_NAME"] || "com.appfactory.qrbarcodescanner")

# Account label for the factory pipeline.
# account: android_001
